<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:SMT"
        xmlns:eveData="clr-namespace:SMT.EVEData;assembly=EVEData"
        xmlns:dotNetKitControls="clr-namespace:DotNetKit.Windows.Controls;assembly=DotNetKit.Wpf.AutoCompleteComboBox"
        xmlns:xcad="clr-namespace:AvalonDock;assembly=AvalonDock"
        xmlns:xcadl="clr-namespace:AvalonDock.Layout;assembly=AvalonDock"
        xmlns:xcadt="clr-namespace:AvalonDock.Themes;assembly=AvalonDock.Themes.Expression"
        x:Class="SMT.MainWindow"
        mc:Ignorable="d"
        Title="SMT" Height="850" Width="1124" Icon="smt_bomb_icon.ico" ResizeMode="CanResizeWithGrip" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextFormattingMode="Ideal">

    <Window.Resources>
        <local:ZKBBackgroundConverter x:Key="zkbBGConverter" />
        <local:ZKBForegroundConverter x:Key="zkbBGFConverter" />
        <local:TimeSpanConverter x:Key="timeSpanConverter" />
        <local:TimeSinceConverter x:Key="TimeSinceConverter" />

        <ObjectDataProvider x:Key="navigationEnumData" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="eveData:RoutingMode" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <DataTemplate x:Key="intelDataDataTemplate">
            <DockPanel>
                <TextBlock Foreground="Cyan" Padding="0,0,5,0" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top">
                    <Run Text="[" /><Run Text="{Binding IntelChannel, FallbackValue=''}" /><Run Text="]" />
                </TextBlock>
                <TextBlock Text="{Binding IntelTime, FallbackValue=TIME, StringFormat=HH:mm}" Foreground="{StaticResource WarningBrush}" Padding="0,0,5,0" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top" />
                <TextBlock Text="{Binding IntelString, FallbackValue=Intel Data}" Foreground="{StaticResource ForegroundBrush}" TextWrapping="WrapWithOverflow" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top" />
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="gameLogDataDataTemplate">
            <DockPanel>
                <TextBlock Foreground="Cyan" Padding="0,0,5,0" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top">
                    <Run Text="[" /><Run Text="{Binding Character, FallbackValue=''}" /><Run Text="]" />
                </TextBlock>
                <TextBlock Text="{Binding Time, FallbackValue=TIME, StringFormat=HH:mm}" Foreground="{StaticResource WarningBrush}" Padding="0,0,5,0" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top" />
                <TextBlock Text="{Binding Text, FallbackValue=GameLog Data}" Foreground="{StaticResource ForegroundBrush}" TextWrapping="WrapWithOverflow" TextBlock.FontSize="{Binding MapConf.IntelTextSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" VerticalAlignment="Top" />
            </DockPanel>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="{DynamicResource MenuBackground}" Height="Auto">
            <Menu Style="{DynamicResource ResourceKey={x:Type Menu}}" Height="Auto">
                <MenuItem Header="{DynamicResource Menu_File}" Height="Auto">
                    <MenuItem Header="{DynamicResource Main_Menu_Characters}" Click="Characters_MenuItem_Click" />
                    <MenuItem Header="{DynamicResource Menu_Preferences}" Click="Preferences_MenuItem_Click" />
                    <MenuItem Header="{DynamicResource Main_Menu_Overlay}" Click="OverlayWindow_MenuItem_Click" />
                    <MenuItem Header="{DynamicResource Main_Menu_Exit}" Click="Exit_MenuItem_Click" />
                </MenuItem>

                <MenuItem Header="{DynamicResource Main_Menu_Data}"  Height="Auto">
                    <MenuItem Header="{DynamicResource Main_Menu_ManageInfra}" Click="ManageInfrastructureUpgrades_Click" />
                    <MenuItem Header="{DynamicResource Main_Menu_LoadInfra}" Click="LoadInfrastructureUpgrades_Click" />
                    <MenuItem Header="{DynamicResource Main_Menu_SaveInfra}" Click="SaveInfrastructureUpgrades_Click" />
                    <Separator />
                    <MenuItem Header="{DynamicResource Main_Menu_ClearLogs}" Click="ClearOldEVELogs_Click" />
                </MenuItem>

                <MenuItem Header="{DynamicResource Main_Menu_View}" Height="Auto">
                    <MenuItem x:Name="miFullScreenToggle" Header="{DynamicResource Main_Menu_FullScreen}" IsCheckable="True" IsChecked="False" Click="FullScreenToggle_MenuItem_Click" />
                    <MenuItem x:Name="miResetLayout" Header="{DynamicResource Main_Menu_ResetLayout}" Click="miResetLayout_Click" />
                    <MenuItem x:Name="miToggleOverlayClickTrough" Header="{DynamicResource Main_Menu_ToggleOverlay}" Click="OverlayClickTroughToggle_MenuItem_Click" />
                </MenuItem>
            </Menu>

            <Label Content="{DynamicResource Main_ActiveChar}" Margin="20,0,0,0" Padding="8,4" VerticalAlignment="Center" />

            <ComboBox x:Name="CurrentActiveCharacterCombo" Width="200"  IsReadOnly="True" SelectionChanged="CurrentActiveCharacterCombo_Selected"  Height="24" VerticalAlignment="Center" />
        </StackPanel>

        <StatusBar x:Name="AppStatusBar" DockPanel.Dock="Bottom" Height="Auto">



            <!-- ── Server online indicator dot ── -->

            <StatusBarItem Margin="8,0,4,0">
                <Ellipse x:Name="ServerOnlineDot" Width="8" Height="8">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource SubtleTextBrush}" />
                            <Setter Property="ToolTip" Value="TQ Offline" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                    <Setter Property="Fill" Value="{StaticResource SafeBrush}" />
                                    <Setter Property="ToolTip" Value="TQ Online" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
            </StatusBarItem>



            <!-- ── TQ label ── -->

            <StatusBarItem Margin="0,0,0,0">

                <TextBlock Text="TQ" Foreground="{StaticResource SubtleTextBrush}"

                           FontSize="{StaticResource FontSizeXS}"

                           VerticalAlignment="Center" />

            </StatusBarItem>



            <!-- ── Separator ── -->

            <Separator />



            <!-- ── EVE Time ── -->

            <StatusBarItem>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <TextBlock Text="{DynamicResource Main_EveTime}"

                               Foreground="{StaticResource SubtleTextBrush}"

                               Margin="0,0,4,0" />

                    <TextBlock Text="{Binding ServerTime, StringFormat=HH:mm}"

                               Foreground="{StaticResource AccentLightBrush}"

                               FontWeight="SemiBold" />

                </StackPanel>

            </StatusBarItem>



            <!-- ── Separator ── -->

            <Separator />



            <!-- ── Version ── -->

            <StatusBarItem>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <TextBlock Text="v" Foreground="{StaticResource MutedTextBrush}"

                               FontSize="{StaticResource FontSizeXS}"

                               Margin="0,0,1,0" />

                    <TextBlock Text="{Binding ServerVersion}"

                               Foreground="{StaticResource SubtleTextBrush}"

                               FontSize="{StaticResource FontSizeXS}" />

                </StackPanel>

            </StatusBarItem>



            <!-- ── Separator ── -->

            <Separator />



            <!-- ── Players badge ── -->

            <StatusBarItem>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <Border Background="{StaticResource HotBrush}"

                            CornerRadius="3" Padding="5,1,5,1" Margin="0,0,5,0">

                        <TextBlock Text="{Binding NumPlayers, StringFormat=N0}"

                                   Foreground="{StaticResource ForegroundBrush}"

                                   FontSize="{StaticResource FontSizeXS}"

                                   FontWeight="SemiBold" />

                    </Border>

                    <TextBlock Text="{DynamicResource Main_Players}"

                               Foreground="{StaticResource SubtleTextBrush}" />

                </StackPanel>

            </StatusBarItem>



        </StatusBar>

        <xcad:DockingManager x:Name="dockManager">

            <xcad:DockingManager.Theme>

                <xcadt:ExpressionDarkTheme />

            </xcad:DockingManager.Theme>



            <!-- Override AvalonDock Expression Dark base colors → Space Dark palette -->

            <xcad:DockingManager.Resources>

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor1" Color="#161b22" />  <!-- panel bg (was #444444) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor2" Color="#e6edf3" />  <!-- foreground text (was White) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor3" Color="#0d1117" />  <!-- deep bg / black (was Black) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor4" Color="#0d1117" />  <!-- dark bg (was #333333) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor5" Color="#161b22" />  <!-- mid bg (was #3D3D3D) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor8" Color="#21262d" />  <!-- light bg / tabs (was #595959) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor9" Color="#1c2128" />  <!-- hover (was #4A4A4A) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor10" Color="#21262d" /> <!-- selected (was #595959) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor11" Color="#1f6feb" /> <!-- highlight/active (was #BDBDBD) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor13" Color="#30363d" /> <!-- lighter surface (was #666666) -->

                <SolidColorBrush x:Key="AvalonDock_Expression_NavigatorWindowListBoxItemHoverFill" Color="#1c2128" />

                <SolidColorBrush x:Key="AvalonDock_Expression_NavigatorWindowListBoxItemSelectedFill" Color="#1f6feb" />

                <SolidColorBrush x:Key="AvalonDock_Expression_NavigatorWindowListBoxItemSelectedInactiveFill" Color="#30363d" />

                <SolidColorBrush x:Key="AvalonDock_Expression_NavigatorWindowListBoxItemSelectedHoverFill" Color="#1f6feb" />



                <!-- Simplify chrome: thinner borders, subtler separators -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor6" Color="#21262d" />  <!-- border / separator -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor7" Color="#161b22" />  <!-- grip area bg -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor12" Color="#30363d" /> <!-- tab border -->

                <SolidColorBrush x:Key="AvalonDock_Expression_BaseColor14" Color="#0d1117" /> <!-- deep dock bg -->



            </xcad:DockingManager.Resources>



            <xcadl:LayoutRoot>
                <xcadl:LayoutRoot.RightSide>
                    <xcadl:LayoutAnchorSide>
                        <xcadl:LayoutAnchorGroup>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_Anoms}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="AnomsContentID" AutoHideMinHeight="200" AutoHideMinWidth="280" x:Name="avlpAnoms">
                                <Grid x:Name="MainAnomGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0">
                                        <Label Content="{Binding ANOMManager.ActiveSystem.SystemName}" FontSize="18" FontWeight="Bold" />
                                    </StackPanel>
                                    <DataGrid Grid.Row="1" x:Name="AnomSigList" Focusable="True" AreRowDetailsFrozen="True" AutoGenerateColumns="False" ItemsSource="{Binding ANOMManager.ActiveSystem.Anoms.Values}" HeadersVisibility="Column" IsReadOnly="True" GridLinesVisibility="Horizontal" Margin="0, 0, 0, 8" PreviewMouseDown="AnomSigList_PreviewMouseDown" PreviewKeyDown="AnomSigList_PreviewKeyDown">
                                        <DataGrid.Resources>
                                            <SolidColorBrush x:Key="SelectedBackgroundColor" Color="{DynamicResource {x:Static SystemColors.HighlightColorKey}}" />
                                            <SolidColorBrush x:Key="SelectedForegroundColor" Color="{DynamicResource {x:Static SystemColors.HighlightTextColorKey}}" />
                                        </DataGrid.Resources>
                                        <DataGrid.CellStyle>
                                            <Style TargetType="{x:Type DataGridCell}">
                                                <Setter Property="BorderThickness" Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="Unknown">
                                                        <Setter Property="Foreground" Value="Red" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Wormhole">
                                                        <Setter Property="Foreground" Value="DeepSkyBlue" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Relic Site">
                                                        <Setter Property="Foreground" Value="Gold" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Data Site">
                                                        <Setter Property="Foreground" Value="LightBlue" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Combat Site">
                                                        <Setter Property="Foreground" Value="Tomato" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Gas Site">
                                                        <Setter Property="Foreground" Value="LightGreen" />
                                                    </DataTrigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource SelectedBackgroundColor}" />
                                                        <Setter Property="Foreground" Value="{StaticResource SelectedForegroundColor}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGrid.CellStyle>
                                        <DataGrid.RowStyle>
                                            <Style TargetType="DataGridRow">
                                                <Setter Property="BorderThickness" Value="0" />
                                                <Setter Property="Background" Value="Transparent" />
                                            </Style>
                                        </DataGrid.RowStyle>
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{DynamicResource Main_Anom_ColSig}" Binding="{Binding Signature}" Width="60" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Anom_ColType}" Binding="{Binding Type}" Width="75" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Anom_ColName}" Binding="{Binding Name}" Width="*" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Anom_ColSince}" Binding="{Binding TimeFound, Converter={StaticResource TimeSinceConverter}}" Width="Auto" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <UniformGrid Grid.Row="2" Columns="2" Margin="8,0,8,8">
                                        <Button x:Name="btnUpdateAnomList" Content="{DynamicResource Main_Anom_BtnUpdate}" Click="btnUpdateAnomList_Click" Margin="0,0,4,0" Height="32" />
                                        <Button x:Name="btnDeleteSelectedAnoms" Content="{DynamicResource Main_Anom_BtnDelete}" Click="btnDeleteSelectedAnoms_Click" Margin="4,0,0,0" Height="32" />
                                    </UniformGrid>
                                    <Button Grid.Row="3" x:Name="btnClearAnomList" Content="{DynamicResource Main_Anom_BtnClear}" Click="btnClearAnomList_Click" Margin="8,0,8,8" Height="32" />
                                </Grid>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_Characters}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="CharactersContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <ScrollViewer Background="{DynamicResource DarkBackgroundBrush}">
                                    <StackPanel>
                                        <DataGrid  x:Name="CharactersList" Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False" MouseDoubleClick="CharactersList_MouseDoubleClick" HeadersVisibility="Column" SelectionUnit="FullRow" GridLinesVisibility="None" IsSynchronizedWithCurrentItem="False" ItemsSource="{Binding Path=., Mode=OneWay, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}">
                                            <DataGrid.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsOnline}" Value="False">

                                                            <Setter Property="Background" Value="{StaticResource HotBrush}" />

                                                            <Setter Property="Foreground" Value="{StaticResource SubtleTextBrush}" />

                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding WarningState}" Value="Warning">

                                                            <Setter Property="Background" Value="{StaticResource WarningBrush}" />

                                                            <Setter Property="Foreground" Value="#0d1117" />

                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGrid.CellStyle>
                                            <DataGrid.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="{DynamicResource Main_Char_CtxDelete}" Click="CharactersListMenuItemDelete_Click" />
                                                </ContextMenu>
                                            </DataGrid.ContextMenu>
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="{DynamicResource Main_Char_ColName}" Binding="{Binding Name}" CanUserReorder="False" Width="*" IsReadOnly="True" />
                                                <DataGridTextColumn Header="{DynamicResource Main_Char_ColLoc}" Binding="{Binding Location}" CanUserReorder="False" Width="*" IsReadOnly="True" />
                                                <DataGridTextColumn Header="{DynamicResource Main_Char_ColReg}" Binding="{Binding Region}" CanUserReorder="False" Width="*" IsReadOnly="True" />
                                            </DataGrid.Columns>
                                        </DataGrid>
                                        <CheckBox Content="{DynamicResource Main_Char_ChkShow}" IsChecked="{Binding MapConf.ShowCharacterNamesOnMap}" Margin="3" />
                                        <Button x:Name="btn_AddCharacter" Click="btn_AddCharacter_Click" Content="{DynamicResource Main_Char_BtnAdd}" />
                                    </StackPanel>
                                </ScrollViewer>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_Route}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="RouteContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <Grid x:Name="MainRouteGrid">
                                    <ScrollViewer Background="{DynamicResource DarkBackgroundBrush}">
                                        <StackPanel>
                                            <GroupBox Header="{DynamicResource Main_Rte_GrpNav}">
                                                <StackPanel Margin="4">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Label Content="{DynamicResource Main_Rte_LblMode}" />
                                                        <ComboBox ItemsSource="{Binding Source={StaticResource navigationEnumData}}" SelectedItem="{Binding ActiveCharacter.NavigationMode, ElementName=RegionUC, Mode=TwoWay}" Margin="2" Width="150" />
                                                    </StackPanel>
                                                    <CheckBox Content="{DynamicResource Main_Rte_ChkAnsiblex}" IsChecked="{Binding ActiveCharacter.UseAnsiblexGates, ElementName=RegionUC}" Margin="2" />
                                                    <CheckBox Content="{DynamicResource Main_Rte_ChkThera}" IsChecked="{Binding ActiveCharacter.UseTheraRouting, ElementName=RegionUC}" Margin="2" />
                                                    <CheckBox Content="{DynamicResource Main_Rte_ChkTurnur}" IsChecked="{Binding ActiveCharacter.UseTurnurRouting, ElementName=RegionUC}" Margin="2" />
                                                    <CheckBox Content="{DynamicResource Main_Rte_ChkZarzakh}" IsChecked="{Binding ActiveCharacter.UseZarzakhRouting, ElementName=RegionUC}" Margin="2" />
                                                </StackPanel>
                                            </GroupBox>
                                            <GroupBox Header="{DynamicResource Main_Rte_GrpWaypoints}">
                                                <StackPanel Margin="4">
                                                    <ListBox x:Name="lbActiveCharacterWaypoints" ItemsSource="{Binding ActiveCharacter.Waypoints, ElementName=RegionUC}" MinHeight="50" Margin="2" />
                                                    <StackPanel Orientation="Horizontal" Margin="2">
                                                        <dotNetKitControls:AutoCompleteComboBox x:Name="RouteSystemDropDownAC" TextSearch.TextPath="LocalizedName" FontSize="12" Width="156" />
                                                        <Button x:Name="AddWaypointsBtn" Click="AddWaypointsBtn_Click"  Content="{DynamicResource Main_Rte_BtnAddWP}" Margin="2" />
                                                    </StackPanel>
                                                    <Button x:Name="ReCalculateRouteBtn" Click="ReCalculateRouteBtn_Click"  Content="{DynamicResource Main_Rte_BtnRecalc}" Margin="2" />
                                                    <Button x:Name="ClearWaypointsBtn" Click="ClearWaypointsBtn_Click" Content="{DynamicResource Main_Rte_BtnClear}" Margin="2" />
                                                </StackPanel>
                                            </GroupBox>

                                            <GroupBox Header="{DynamicResource Main_Rte_GrpRoute}">
                                                <StackPanel Margin="4">
                                                    <DataGrid x:Name="dgActiveCharacterRoute" ItemsSource="{Binding ActiveCharacter.ActiveRoute, ElementName=RegionUC}" MinHeight="50" AutoGenerateColumns="false">
                                                        <DataGrid.CellStyle>
                                                            <Style TargetType="{x:Type DataGridCell}">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding GateToTake}" Value="Thera">
                                                                        <Setter Property="Background" Value="#292" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding GateToTake}" Value="Turnur">
                                                                        <Setter Property="Background" Value="DarkRed" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding GateToTake}" Value="Ansiblex">
                                                                        <Setter Property="Background" Value="blue" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding GateToTake}" Value="Zarzakh">
                                                                        <Setter Property="Background" Value="Purple" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="TriglavianMinorVictory">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="EdencomMinorVictory">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="Fortress">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="EdencomMinorVictory" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="Fortress" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.TrigStandingsGood,ElementName=RegionUC}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="TriglavianMinorVictory" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <DataTrigger Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False">
                                                                        <Setter Property="Background" Value="blue" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </DataGrid.CellStyle>
                                                        <DataGrid.Columns>
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColSys}" Binding="{Binding SystemName}" Width="15*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColCon}" Binding="{Binding GateToTake}" Width="8*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColJumps}" Binding="{Binding ActualSystem.JumpsLastHour}" Width="5*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColSK}" Binding="{Binding ActualSystem.ShipKillsLastHour}" Width="5*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColPK}" Binding="{Binding ActualSystem.PodKillsLastHour}" Width="5*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_Rte_ColTrig}" Binding="{Binding ActualSystem.TrigInvasionStatus}" Width="5*" CanUserSort="False" />
                                                        </DataGrid.Columns>
                                                    </DataGrid>
                                                    <StackPanel Orientation="Horizontal" Margin="2">
                                                        <Label Content="{DynamicResource Main_Rte_LblTotalJumps}" />
                                                        <Label Content="{Binding ActiveCharacter.ActiveRouteLength, ElementName=RegionUC}" />
                                                    </StackPanel>
                                                    <Button x:Name="CopyRouteBtn" Click="CopyRouteBtn_Click" Content="{DynamicResource Main_Rte_BtnCopy}" Margin="2" />
                                                </StackPanel>
                                            </GroupBox>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_JumpPlanner}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="JumpRouteContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <Grid x:Name="MainJumpRouteGrid">
                                    <ScrollViewer Background="{DynamicResource DarkBackgroundBrush}">
                                        <StackPanel>
                                            <GroupBox Header="{DynamicResource Main_JP_GrpNav}">
                                                <StackPanel Margin="4">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Label Width="70" Content="{DynamicResource Main_JP_LblShip}" />
                                                        <ComboBox x:Name="jumpPlannerShipType" SelectionChanged="JumpPlannerShipType_OnSelectionChanged">
                                                            <ComboBoxItem DataContext="6.0" Content="{DynamicResource Main_JP_ShipAnsiblex}" />
                                                            <ComboBoxItem DataContext="6.0" Content="{DynamicResource Main_JP_ShipSuper}" />
                                                            <ComboBoxItem DataContext="7.0" IsSelected="True" Content="{DynamicResource Main_JP_ShipReg}" />
                                                            <ComboBoxItem DataContext="8.0" Content="{DynamicResource Main_JP_ShipBlops}" />
                                                            <ComboBoxItem DataContext="10.0" Content="{DynamicResource Main_JP_ShipJF}" />
                                                        </ComboBox>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Label Width="70" Content="{DynamicResource Main_JP_LblSkills}" />
                                                        <ComboBox x:Name="jumpPlannerJDC" SelectionChanged="JumpPlannerJDC_OnSelectionChanged">
                                                            <ComboBoxItem DataContext="4" Content="{DynamicResource Main_JP_Skill4}" />
                                                            <ComboBoxItem DataContext="5" IsSelected="True" Content="{DynamicResource Main_JP_Skill5}" />
                                                        </ComboBox>
                                                    </StackPanel>
                                                </StackPanel>
                                            </GroupBox>
                                            <GroupBox Header="{DynamicResource Main_JP_GrpWP}">
                                                <StackPanel Margin="4">
                                                    <ListBox x:Name="capitalRouteWaypointsLB" ItemsSource="{Binding CapitalRoute.WayPoints}" MinHeight="50" Margin="2" AllowDrop="True">
                                                        <ListBox.ContextMenu>
                                                            <ContextMenu>
                                                                <MenuItem Header="{DynamicResource Main_JP_CtxMoveUp}" Click="CapitalWaypointsContextMenuMoveUp_Click" />
                                                                <MenuItem Header="{DynamicResource Main_JP_CtxMoveDown}" Click="CapitalWaypointsContextMenuMoveDown_Click" />
                                                                <MenuItem Header="{DynamicResource Main_JP_CtxDelete}" Click="CapitalWaypointsContextMenuDelete_Click" />
                                                            </ContextMenu>
                                                        </ListBox.ContextMenu>
                                                    </ListBox>
                                                    <StackPanel Orientation="Horizontal" Margin="2">
                                                        <dotNetKitControls:AutoCompleteComboBox x:Name="JumpRouteSystemDropDownAC" TextSearch.TextPath="LocalizedName" FontSize="12" Width="156" />
                                                        <Button x:Name="AddJumpWaypointsBtn" Click="AddJumpWaypointsBtn_Click"  Content="{DynamicResource Main_JP_BtnAddWP}" Margin="2" />
                                                    </StackPanel>
                                                    <Button x:Name="ClearJumpWaypointsBtn" Click="ClearJumpWaypointsBtn_Click" Content="{DynamicResource Main_JP_BtnClearWP}" Margin="2" />
                                                </StackPanel>
                                            </GroupBox>

                                            <GroupBox Header="{DynamicResource Main_JP_GrpAvoid}">
                                                <StackPanel Margin="4">
                                                    <ListBox x:Name="capitalRouteAvoidLB"  ItemsSource="{Binding CapitalRoute.AvoidSystems}" MinHeight="50" Margin="2" />
                                                    <StackPanel Orientation="Horizontal" Margin="2">
                                                        <dotNetKitControls:AutoCompleteComboBox x:Name="JumpRouteAvoidSystemDropDownAC" TextSearch.TextPath="LocalizedName" FontSize="12" Width="156" />
                                                        <Button x:Name="AddJumpAvoidSystemsBtn" Click="AddJumpAvoidSystemsBtn_Click"  Content="{DynamicResource Main_JP_BtnAddAvoid}" Margin="2" />
                                                    </StackPanel>
                                                    <Button x:Name="ClearJumpAvoidSystemsBtn" Click="ClearJumpAvoidSystemsBtn_Click" Content="{DynamicResource Main_JP_BtnClearAvoid}" Margin="2" />
                                                </StackPanel>
                                            </GroupBox>

                                            <GroupBox Header="{DynamicResource Main_JP_GrpRoute}">
                                                <StackPanel Margin="4">
                                                    <Label x:Name="lblCapitalRouteSummary" Content="" />
                                                    <DataGrid x:Name="dgCapitalRouteCurrentRoute" ItemsSource="{Binding CapitalRoute.CurrentRoute}" MinHeight="50" AutoGenerateColumns="false" SelectionChanged="CurrentCapitalRouteItem_Selected" IsReadOnly="True">
                                                        <DataGrid.CellStyle>
                                                            <Style TargetType="{x:Type DataGridCell}">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="TriglavianMinorVictory">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="EdencomMinorVictory">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="Fortress">
                                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}" />
                                                                        <Setter Property="Foreground" Value="#0d1117" />
                                                                    </DataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="EdencomMinorVictory" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="Fortress" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding ActiveCharacter.TrigStandingsGood,ElementName=RegionUC}" Value="False" />
                                                                            <Condition Binding="{Binding ActualSystem.TrigInvasionStatus}" Value="TriglavianMinorVictory" />
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Background" Value="Red" />
                                                                    </MultiDataTrigger>
                                                                    <DataTrigger Binding="{Binding ActiveCharacter.EdenCommStandingsGood}" Value="False">
                                                                        <Setter Property="Background" Value="blue" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </DataGrid.CellStyle>
                                                        <DataGrid.Columns>
                                                            <DataGridTextColumn Header="{DynamicResource Main_JP_ColSys}" Binding="{Binding SystemName}" Width="15*" CanUserSort="False" />
                                                            <DataGridTextColumn Header="{DynamicResource Main_JP_ColDist}" Binding="{Binding LY, StringFormat={}{0:#.##}}" Width="8*" CanUserSort="False" />
                                                        </DataGrid.Columns>
                                                    </DataGrid>
                                                </StackPanel>
                                            </GroupBox>
                                            <GroupBox Header="{DynamicResource Main_JP_GrpAlt}">
                                                <StackPanel Orientation="Vertical">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Label Content="{DynamicResource Main_JP_LblAltFor}" />
                                                        <Label x:Name="lblAlternateMids" />
                                                    </StackPanel>
                                                    <ListBox x:Name="lbAlternateMids">
                                                        <ListBox.ContextMenu>
                                                            <ContextMenu>
                                                                <MenuItem Header="{DynamicResource Main_JP_CtxUseAlt}" Click="CapitalRouteContextMenuUseAlt_Click" />
                                                            </ContextMenu>
                                                        </ListBox.ContextMenu>
                                                    </ListBox>
                                                </StackPanel>
                                            </GroupBox>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_WH}" CanClose="False" CanAutoHide="True" CanHide="false" ContentId="TheraContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <TabControl>
                                    <TabItem Header="{DynamicResource Main_WH_TabThera}">
                                        <ScrollViewer>
                                            <StackPanel>
                                                <DataGrid  x:Name="TheraConnectionsList" Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="TheraConnectionsList_MouseDoubleClick" HeadersVisibility="Column" Background="{StaticResource DarkBackgroundBrush}">
                                                    <DataGrid.CellStyle>
                                                        <Style TargetType="{x:Type DataGridCell}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding EstimatedEOL}" Value="critical">
                                                                    <Setter Property="Background" Value="Red" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </DataGrid.CellStyle>
                                                    <DataGrid.Columns>
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColSys}" Binding="{Binding System}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColReg}" Binding="{Binding Region}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColInSig}" Binding="{Binding InSignatureID}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColOutSig}" Binding="{Binding OutSignatureID}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColEOL}" Binding="{Binding EstimatedEOL}" Width="*" />
                                                    </DataGrid.Columns>
                                                </DataGrid>
                                                <Button x:Name="btn_UpdateThera" Click="btn_UpdateThera_Click" Content="{DynamicResource Main_WH_BtnUpdate}" />
                                            </StackPanel>
                                        </ScrollViewer>
                                    </TabItem>
                                    <TabItem Header="{DynamicResource Main_WH_TabTurnur}">
                                        <ScrollViewer>
                                            <StackPanel>
                                                <DataGrid  x:Name="TurnurConnectionsList" Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="TurnurConnectionsList_MouseDoubleClick" HeadersVisibility="Column" Background="{StaticResource DarkBackgroundBrush}">
                                                    <DataGrid.CellStyle>
                                                        <Style TargetType="{x:Type DataGridCell}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding EstimatedEOL}" Value="critical">
                                                                    <Setter Property="Background" Value="Red" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </DataGrid.CellStyle>
                                                    <DataGrid.Columns>
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColSys}" Binding="{Binding System}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColReg}" Binding="{Binding Region}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColInSig}" Binding="{Binding InSignatureID}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColOutSig}" Binding="{Binding OutSignatureID}" Width="*" />
                                                        <DataGridTextColumn Header="{DynamicResource Main_WH_ColEOL}" Binding="{Binding EstimatedEOL}" Width="*" />
                                                    </DataGrid.Columns>
                                                </DataGrid>
                                                <Button x:Name="btn_UpdateTurnur" Click="btn_UpdateTurnur_Click" Content="{DynamicResource Main_WH_BtnUpdate}" />
                                            </StackPanel>
                                        </ScrollViewer>
                                    </TabItem>
                                </TabControl>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_Storms}" CanClose="False" CanAutoHide="True" CanHide="false" ContentId="StormContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <ScrollViewer>
                                    <StackPanel>
                                        <DataGrid  x:Name="MetaliminalStormList" Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="{StaticResource DarkBackgroundBrush}" MouseDoubleClick="MetaliminalStormList_MouseDoubleClick">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="{DynamicResource Main_Stm_ColSys}" Binding="{Binding System}" Width="*" />
                                                <DataGridTextColumn Header="{DynamicResource Main_Stm_ColReg}" Binding="{Binding Region}" Width="*" />
                                                <DataGridTextColumn Header="{DynamicResource Main_Stm_ColType}" Binding="{Binding Type}" Width="*" />
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </ScrollViewer>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_SOV}" CanClose="False" CanAutoHide="True" CanHide="false" ContentId="SOVCampaignsID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <ScrollViewer>
                                    <StackPanel>
                                        <CheckBox Content="{DynamicResource Main_SOV_ChkRegOnly}" x:Name="SovCampaignFilterViewChk" IsChecked="False" Checked="SovCampaignFilterViewChk_Checked" Unchecked="SovCampaignFilterViewChk_Checked" Margin="4" />
                                        <DataGrid x:Name="SovCampaignList" Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="SovCampaignList_MouseDoubleClick" HeadersVisibility="Column">
                                            <DataGrid.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Path=IsActive, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                                            <Setter Property="Background" Value="DarkRed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGrid.CellStyle>
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="{DynamicResource Main_SOV_ColSys}" Binding="{Binding Path=System, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                                                <DataGridTextColumn Header="{DynamicResource Main_SOV_ColReg}" Binding="{Binding Path=Region, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                                                <DataGridTemplateColumn  Header="{DynamicResource Main_SOV_ColScore}"  Width="*">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <Grid>
                                                                <ProgressBar Value="{Binding Path=DefendersScore, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0" Maximum="1"  Height="13" ToolTipService.ToolTip="{Binding State}" ToolTipService.InitialShowDelay="100" ToolTipService.BetweenShowDelay="100" />
                                                                <TextBlock Text="{Binding Path=DefendersScore, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource ForegroundBrush}" IsHitTestVisible="False" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>
                                                <DataGridTextColumn Header="{DynamicResource Main_SOV_ColStarts}" Binding="{Binding Path=TimeToStart, Mode=OneTime, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource timeSpanConverter}}" Width="*" />
                                                <DataGridTextColumn Header="{DynamicResource Main_SOV_ColDef}" Binding="{Binding Path=DefendingAllianceName, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </ScrollViewer>
                            </xcadl:LayoutAnchorable>
                        </xcadl:LayoutAnchorGroup>
                    </xcadl:LayoutAnchorSide>
                </xcadl:LayoutRoot.RightSide>

                <xcadl:LayoutPanel>
                    <xcadl:LayoutAnchorablePaneGroup DockWidth="250" DockMinWidth="250" Orientation="Vertical">
                        <xcadl:LayoutAnchorablePane>
                            <xcadl:LayoutAnchorable x:Name="ZKBLayoutAnchorable" Title="{DynamicResource Main_Panel_ZKB}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="ZKBContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <Grid>
                                    <ScrollViewer Background="{DynamicResource DarkBackgroundBrush}">
                                        <StackPanel CanVerticallyScroll="True" ScrollViewer.CanContentScroll="True">
                                            <CheckBox Content="{DynamicResource Main_ZKB_ChkRegOnly}" x:Name="ZKBFeedFilterViewChk" IsChecked="True" Checked="ZKBFeedFilterViewChk_Checked" Unchecked="ZKBFeedFilterViewChk_Checked" Margin="4" />
                                            <DataGrid x:Name="ZKBFeed" Grid.Row="1" AreRowDetailsFrozen="True" Cursor="Hand" AutoGenerateColumns="False" CanUserAddRows="False" MouseUp="ZKBContexMenu_ShowSystem_Click" MouseDoubleClick="ZKBFeed_MouseDoubleClick_1" HeadersVisibility="Column" SelectionUnit="FullRow" GridLinesVisibility="None"  MinHeight="200">
                                                <DataGrid.Columns>
                                                    <!-- System: accent blue #58a6ff -->
                                                    <DataGridTemplateColumn Width="30*" IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                                                        <DataGridTemplateColumn.Header>
                                                            <TextBlock Text="{DynamicResource ZKB_Column_System}" />
                                                        </DataGridTemplateColumn.Header>
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding SystemName}" Foreground="#58a6ff" Padding="4,0" TextTrimming="CharacterEllipsis" />
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>

                                                    <!-- Alliance: subtle grey #8b949e -->
                                                    <DataGridTemplateColumn Width="40*" IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                                                        <DataGridTemplateColumn.Header>
                                                            <TextBlock Text="{DynamicResource ZKB_Column_Alliance}" />
                                                        </DataGridTemplateColumn.Header>
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding VictimAllianceName}" Foreground="#8b949e" Padding="4,0" TextTrimming="CharacterEllipsis" />
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>

                                                    <!-- Ship: warning orange #f0883e -->
                                                    <DataGridTemplateColumn Width="30*" IsReadOnly="True" CanUserReorder="False" CanUserSort="False">
                                                        <DataGridTemplateColumn.Header>
                                                            <TextBlock Text="{DynamicResource ZKB_Column_ShipType}" />
                                                        </DataGridTemplateColumn.Header>
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding ShipType}" Foreground="#f0883e" Padding="4,0" TextTrimming="CharacterEllipsis" />
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                                <DataGrid.RowStyle>
                                                    <Style TargetType="{x:Type DataGridRow}">
                                                        <Setter Property="Background" Value="{Binding Converter={StaticResource zkbBGConverter}}" />
                                                        <Setter Property="FontSize" Value="10" />
                                                    </Style>
                                                </DataGrid.RowStyle>
                                                <DataGrid.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="{DynamicResource Main_ZKB_CtxShowSys}" Click="ZKBContexMenu_ShowSystem_Click" />
                                                        <MenuItem Header="{DynamicResource Main_ZKB_CtxOpen}" Click="ZKBContexMenu_ShowZKB_Click" />
                                                    </ContextMenu>
                                                </DataGrid.ContextMenu>
                                            </DataGrid>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </xcadl:LayoutAnchorable>
                        </xcadl:LayoutAnchorablePane>

                        <xcadl:LayoutAnchorablePane>
                            <xcadl:LayoutAnchorable x:Name="IntelLayoutAnchorable" Title="{DynamicResource Main_Panel_Intel}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="IntelContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="25" />
                                    </Grid.RowDefinitions>
                                    <ListView x:Name="RawIntelBox" Margin="0,2" MouseDoubleClick="RawIntelBox_MouseDoubleClick" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="0" ItemTemplate="{StaticResource intelDataDataTemplate}" />
                                    <Button Content="{DynamicResource Main_Int_BtnClear}" Grid.Row="1" x:Name="ClearIntelBtn" Click="ClearIntelBtn_Click" />
                                </Grid>
                            </xcadl:LayoutAnchorable>
                            <xcadl:LayoutAnchorable x:Name="GamelogLayoutAnchorable" Title="{DynamicResource Main_Panel_Gamelog}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="GameLogContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="25" />
                                    </Grid.RowDefinitions>
                                    <ListView x:Name="RawGameDataBox" Margin="0,2" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="0" ItemTemplate="{StaticResource gameLogDataDataTemplate}" />
                                    <Button Content="{DynamicResource Main_Log_BtnClear}" Grid.Row="1" x:Name="ClearGameLogBtn" Click="ClearGameLogBtn_Click" />
                                </Grid>
                            </xcadl:LayoutAnchorable>

                            <xcadl:LayoutAnchorable Title="{DynamicResource Main_Panel_Fleet}" CanClose="False" CanAutoHide="True" CanHide="False" ContentId="FleetContentID" AutoHideMinHeight="200" AutoHideMinWidth="280">
                                <ScrollViewer Background="{DynamicResource DarkBackgroundBrush}">
                                    <DataGrid  x:Name="FleetMembersList"  Grid.Row="1" AreRowDetailsFrozen="True" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MouseDoubleClick="CharactersList_MouseDoubleClick" HeadersVisibility="Column" SelectionUnit="FullRow" GridLinesVisibility="None" IsSynchronizedWithCurrentItem="False">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{DynamicResource Main_Flt_ColName}" Binding="{Binding Name}" CanUserReorder="False" Width="*" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Flt_ColLoc}" Binding="{Binding Location}" CanUserReorder="False" Width="*" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Flt_ColReg}" Binding="{Binding Region}" CanUserReorder="False" Width="*" />
                                            <DataGridTextColumn Header="{DynamicResource Main_Flt_ColShip}" Binding="{Binding ShipType}" CanUserReorder="False" Width="*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </xcadl:LayoutAnchorable>
                        </xcadl:LayoutAnchorablePane>
                    </xcadl:LayoutAnchorablePaneGroup>

                    <xcadl:LayoutDocumentPane ShowHeader="True" AllowDuplicateContent="False">
                        <xcadl:LayoutDocument CanClose="False" CanFloat="True" ContentId="UniverseContentID" Title="{DynamicResource Main_Panel_Regions}">
                            <Grid>
                                <local:RegionsViewControl x:Name="RegionsViewUC" />
                            </Grid>
                        </xcadl:LayoutDocument>

                        <xcadl:LayoutDocument CanClose="False" CanFloat="True" ContentId="MapRegionContentID" Title="{DynamicResource Main_Panel_Region}">
                            <local:RegionControl x:Name="RegionUC" />
                        </xcadl:LayoutDocument>

                        <xcadl:LayoutDocument CanClose="False" CanFloat="True" ContentId="FullUniverseViewID" Title="{DynamicResource Main_Panel_Universe}">
                            <local:UniverseControl x:Name="UniverseUC" />
                        </xcadl:LayoutDocument>
                    </xcadl:LayoutDocumentPane>
                </xcadl:LayoutPanel>
            </xcadl:LayoutRoot>
        </xcad:DockingManager>
    </DockPanel>
</Window>